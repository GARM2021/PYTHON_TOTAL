20220429
strings(str) "hola", "%$&", " ", "123"
integer(int) 150, 1, 555
floating(float) 1.25, 25.0, 500.50
listas(lst) ["sal", 1, -3, 4.5, "marte", 0] -> pueden ser de diferentes tipos, tienen un orden en un indice 
diccionarios(dic) {'color':'rojo', 'arte':'cine'} cada elemento tiene dos clave y valor 
tuples(tup) ('lun','mar','mie','jue','vie')  -> su orden no se altera
sets(set) {'a', 'b', 'c', 'd', 'e'} -> tienen elementos unicos que no se pueden repetir

booleanos(bool)

strings
upper() -pasar mayusculas
lower() - pasar minusculas
split () -separalo en partes(lista)
join() -unir items de una lista usando separador
find() -encontrar un sub-string devuelve el indice si encuentra devuelve -1
replace() -reemplazar un substring

